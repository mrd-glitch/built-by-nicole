Square filter chip — programme filters, muscle groups, applied search facets.

```jsx
<Tag selected onSelect={() => {}}>Strength</Tag>
<Tag onRemove={() => {}}>Glutes</Tag>
```

Selected state is ink-filled, not pink — pink is reserved for actions. Use `Badge` instead when the chip is read-only status.
