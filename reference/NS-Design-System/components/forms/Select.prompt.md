Dropdown for closed lists — programme length, experience level, timezone.

```jsx
<Select label="Goal" options={["Build muscle", "Cut", "First show", "Get strong"]} />
```

Shares the `Input` shell, label and error behaviour. Chevron is a 2px square-cap stroke to match the Lucide set.
