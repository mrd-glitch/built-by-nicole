Names an icon-only control or explains a metric abbreviation.

```jsx
<Tooltip label="Rate of perceived exertion"><IconButton label="About RPE" variant="ghost" size="sm"><Icon name="info" size={16} /></IconButton></Tooltip>
```

Fades in over 140ms, no movement. Never put a link or a button inside one.
